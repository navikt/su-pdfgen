<!DOCTYPE html>
<html lang="no">

<head>
    <meta charset="UTF-8"/>
    <meta name="description" content="Supplerende Stønad"/>
    <meta name="author" content="su-pdfgen"/>
    <meta name="subject" content="Supplerende Stønad"/>
    <style>
        * {
            font-family: "Source Sans Pro", SourceSansPro, Source_Sans_Pro, ArialSystem, sans-serif;
        }

        @page {
            @bottom-right {
                font-family: "Source Sans Pro", SourceSansPro, Source_Sans_Pro, ArialSystem, sans-serif;
                font-size: 12px;
                content: "Side " counter(page) " av " counter(pages);
            }
        }

        .header {
            margin-bottom: 40px;
        }

        .navLogo {
            height: 75px;
            margin-bottom: 128px;
        }

        .personalia {
            clear: both;
            padding-bottom: 16px;
        }

        .personalia p {
            margin: 0;
        }

        .mainContent {
            margin-bottom: 40px;
        }

        .pagebreak {
            page-break-before: always;
        }

        .info {
            margin-bottom: 1rem;
        }

        .shortSpacedInfo  {
            margin-bottom: 1rem;
        }

        .shortSpacedInfo p {
            padding: 0 0 3px 0;
            margin: 0 0 3px 0;
        }

        .shortSpacedInfo h3 {
            padding: 0 0 3px 0;
            margin: 0 0 3px 0;
        }

        .shortSpacedInfo h4 {
            padding: 0 0 3px 0;
            margin: 0 0 3px 0;
        }

        .info p {
            padding: 0;
            margin: 0;
        }

        .info ul {
            padding-top: 0;
            padding-bottom: 0;
            margin-top: 0;
            margin-bottom: 0;
        }

        h3 {
            margin-bottom: 4px;
        }

        .hilsen {
            margin-top: 64px;
        }

        .hilsen p {
            padding: 0;
            margin: 0;
        }

        .keyValueOppsummering__heading {
            margin-bottom: 0;
        }

        .keyValueOppsummering {
            border-collapse: collapse;
            width: 100%;
            clear: both;
        }

        .keyValueOppsummering__title {
            margin-bottom: 0;
        }

        .keyValueOppsummering__value {
            text-align: right;
        }

        .keyValueOppsummering__keyInfo {
            list-style: none;
            margin: 0;
            padding-left: 16px;
            font-size: 0.8em;
        }

        .fritekst {
            white-space: pre-wrap;
            margin-bottom: 1rem;
            word-wrap: break-word;
        }

        .preline {
            white-space: pre-line;
        }

        .tabell {
            width: 100%;
            border-collapse: collapse;
            table-layout: fixed;
        }

        .tabell-auto {
            width: 100%;
            border-collapse: collapse;
            table-layout: auto;
        }

        .tabellcol {
            border: 1px solid black;
            padding-left: 0.2em;
            padding-right: 0.2em;
        }

        .boldedText {
            font-weight: bold
        }

        .listItem-minor-spaced {
            margin-bottom: 5px;
        }


    </style>
    <title>Supplerende stønad</title>
</head>

<body>
<div class="main">
    {{#with personalia}}
        <div class="header">
            <img class="navLogo" src="{{image "Navlogo.png"}}" alt="NAV logo"/>

            <div class="personalia">
                <p>{{fornavn}} {{etternavn}}</p>
                <p>Dato: {{dato}}</p>
                <p>Saksnummer: {{saksnummer}}</p>
            </div>
        </div>
    {{/with}}
    {{> @partial-block}}

</div>
</body>

</html>
